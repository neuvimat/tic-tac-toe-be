// This is the main configuration file with common configuration options
// If config.local.js is found, it will be merged with the config.js, the .local version taking precedence
// and overriding the overall config.js
module.exports = {
  PORT: 5001
}
